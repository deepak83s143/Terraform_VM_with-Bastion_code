name: "dd"
on: workflow_dispatch
jobs:
  terraform-job:
     runs-on: Linux-MaDeMi
     steps:
       - name: checkout repo
         uses: actions/checkout@v3
       - name: Terraform Validation
         uses: tf-actions/validate@v1.0.0
         with:
           init: true
                
     
  
